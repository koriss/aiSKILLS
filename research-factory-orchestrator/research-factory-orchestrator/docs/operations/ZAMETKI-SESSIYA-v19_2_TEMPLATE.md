# ZAMETKI-SESSIYA — v19.2.0 handoff template

> Copy relevant sections into the host note file (e.g. `/opt/openclaw/ZAMETKI-SESSIYA.md`)
> after operator-approved install.

## Deliverables

- Workspace zip: `_guests/openclaw-research-factory-orchestrator-v19.2.0-runtime-truth-restoration-workspace.zip`
- SHA256: read from `release-manifest.json` **inside** the zip (`release-artifacts/` build)
- Git branch: `feat/v19-2-0-runtime-truth`
- Honesty loop summary: iterations run of `scripts/verify_skill_run_claims.py` (compat wrapper `verify_openclaw_run.py`, max 3)

## Loop iterations (fill in)

1. …
2. …
3. …

## Slash-command sanity (host)

- …
