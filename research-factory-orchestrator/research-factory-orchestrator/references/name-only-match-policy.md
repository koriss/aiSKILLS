
# Name-Only Match Policy

A profile found only by matching name must be treated as candidate or rejected.

Forbidden:

- using name-only profile to infer employment;
- using name-only profile to infer education;
- using name-only profile to infer family;
- using name-only profile to infer location;
- using name-only profile as source for target-person claims.
