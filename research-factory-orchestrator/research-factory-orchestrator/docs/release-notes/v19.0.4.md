# Release notes — v19.0.4

**Theme:** Boundary contract refresh + CLI smoke gate + corrupt-render rollback Reality Check + release-transcript policy.

## Summary

| Area | Change |
|------|--------|
| **Boundary** | `contracts/core-boundary-contract.json` → **19.0.4**: documents `runtime/*_impl.py`, `runtime/cli.py`, deprecated `runtime/impl.py` shim, legacy `scripts/rfo_v18_core.py` facade. |
| **Package doc** | `runtime/__init__.py` docstring aligned with bounded-module layout (no stale v18.3.2 monolith wording). |
| **Release gates** | `scripts/validate_release.py`: **`smoke_cli_v19`** (`smoke --provider cli --interface direct_runtime` under `RFO_V19_PROFILE=mvr`) + **`_smoke_corrupt_render`** (legacy HTML gate → fail-closed rollback stubs). Both in **`REQUIRED_GATES`**. |
| **ADR** | **`docs/adr/ADR-011-release-validation-transcript.md`** — `release-validation-transcript.json` is **committed** as proof artifact (not `.gitignore`). |
| **Version** | `runtime/version.json`: `skill_version` **19.0.4**, `runtime_contract` **19.0.4**; `failure_corpus_index_version` unchanged **19.0.3** (corpus stable). |

## v18.6 plan closure (back-reference)

Items from `rfo_v18_6_runtime_boundary_and_publish_profile` addressed in this release:

- **G1** `core_boundary_contract_refresh` → this contract refresh.  
- **G2** `gate_smoke_cli_in_validate_release` → `smoke_cli_v19` gate.  
- **G4** `gitignore_release_transcript` → ADR-011 policy (committed transcript).  
- **G5** `gate_corrupt_smoke_doc_or_script` → `scripts/_smoke_corrupt_render.py` + REQUIRED_GATES.

## Operator notes

- Run full verify chain before tagging (see `scripts/validate_release.py` docstring / ADR-011).  
- CLI smoke uses the same post-smoke closure checks as Telegram v19 (`_v19_post_smoke_ok`).

## Semver

Patch bump only; runtime behavior for happy paths unchanged. New gates may fail CI if environment cannot run smokes — that is intentional (release honesty).
