# RFO v19.4.0 — standalone full-research + worker packaging

- Bump skill/runtime semver to **19.4.0** (single source: `runtime/version.json`).
- **`scripts/run_rfo_full_research.py`:** relay fanout, `graph/wave-plan.json`, citation grounding before outbox, honest `final-answer-gate.json`, improved IO / narrative heuristics.
- **`runtime_job_worker`:** `ensure_pkg_required_paths` before `build_package` in `cmd_worker`.

See [`CHANGELOG.md`](../../CHANGELOG.md) § 19.4.x for the full list.
