# Example: report factory (queue)

**User request (paraphrased):** Produce a comparable one-page brief per user-supplied line in an uploaded list file; do not ask me to start each one manually.

**Mode:** `REPORT_FACTORY` + `AUTO_COMPILE_AND_EXECUTE`.

**Expected:** `queue.json` with one item per line; run until all items terminal or `STOP`.
