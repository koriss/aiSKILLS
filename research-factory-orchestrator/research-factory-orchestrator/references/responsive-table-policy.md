
# Responsive Table Policy

Three table modes are allowed:

```text
compact_table
wide_table
cardified_table
```

Rules:

```text
Use compact_table for <= 4 columns.
Use wide_table with horizontal scroll for dense comparisons or source registries.
Use cardified_table only when row-level reading matters more than cross-column comparison.
```
