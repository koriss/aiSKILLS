
# Report Archetype Policy

Use archetypes as structured starting points, not rigid cages.

The agent must:

```text
1. classify report task;
2. select archetype;
3. build block plan;
4. fill semantic model;
5. render standalone HTML.
```

If no archetype fits, use `compose_from_components`.
