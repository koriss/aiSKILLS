{
  "contract_id": "query-fanout-config",
  "version": "v19.3.1",
  "description": "Deterministic relay query templates executed sequentially (stable order), merged/deduped by URL — not a parallel multi-agent swarm. Cap with RFO_QUERY_FANOUT_QUERIES; see docs/design/RFO-SEQUENTIAL-SEARCH-NO-MULTI-AGENT.md.",
  "templates": [
    "{task}",
    "{task} definition background",
    "{task} controversy dispute",
    "{task} recent news timeline",
    "site:wikipedia.org {task}",
    "{task} official site OR government",
    "{task} analysis independent",
    "{task} risks limitations"
  ],
  "default_max_templates": 8
}
