"""Bounded RFO runtime package (v19+).

Implementation lives in ``runtime/*_impl.py`` modules and ``runtime/cli.py`` (CLI entry).

Import concrete entrypoints via ``runtime.cli.main`` or a specific ``*_impl`` module.
"""
