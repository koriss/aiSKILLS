# Release v19.0.0 — Pragmatic Rigor (implementation phase 1)

## Summary

Phase 1 lands **frozen v19 core schemas** under `schemas/core/`, **four validation profiles** at repo root `validation-profiles/`, **six stdlib validators** in `validators/core/`, the **`scripts/run_core_validators.py`** runner plus **`scripts/check_validation_pass.py`**, optional runtime path via **`RFO_V19_PROFILE`** (`mvr` | `full-rigor` | `propaganda-io` | `book-verification`) in `runtime/validate_impl.py`, **`SKILL-core.md`** beside legacy **`SKILL.md`**, migration helper **`scripts/migrate_validator_invocation.py`**, and **`failure-corpus/index-v19.json`**.

v18.5.1 **fail-closed rollback** and v18.7 **logical consistency** (`validate_logical_consistency.py`) remain unchanged for the legacy DAG path.

## ADR

See `docs/adr/ADR-001-v19-pragmatic-rigor.md`.

## Operator notes

- Core validation: `python -S scripts/run_core_validators.py --run-dir <dir> --profile mvr`
- Runtime (optional v19 DAG): export `RFO_V19_PROFILE=mvr` (or other profile) before the validate step that calls `runtime/validate_impl.validate`.
- Fixture regression: `tests/fixtures/v19/good/mvr_minimal_valid` (expect rc 0); each `tests/fixtures/v19/bad/*` (expect rc 1; use `--profile full-rigor` for `l0_scan_unknown_under_full_profile`).

## Artifacts

- **Zip:** `openclaw-research-factory-orchestrator-v19.0.0-pragmatic-rigor-phase1-workspace.zip` in `$HOME/_guests/`.
