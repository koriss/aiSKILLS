# Analytics documentation subtree

**`ARCHIVAL_CONTEXT_ONLY`**

This directory and its descendants are **historical / analytical context** for maintainers. They are **not** operator runbooks for canonical execute unless explicitly linked from `docs/runtime-paths.md` or `SKILL.md`.

- Prefer **`docs/runtime-paths.md`** for entrypoints and production behavior.
- Marketing language must remain **agent-attested** framing; see `scripts/validate_operator_facing_markdown.py` for operator-facing gates.
