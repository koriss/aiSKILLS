# Release notes — v19.2.1 (Honesty Hardening)

> **Current tree:** In-repo Telegram adapters and env-based chat routing are
> **removed**; treat the “Migration” bullets about `TELEGRAM_*` as historical
> unless you maintain a fork that still ships `providers/telegram/`.

## Summary

v19.2.1 closes the policy-vacuum observed in `RUN-36a7dcf7afd7`: the runtime can
no longer silently execute from backup skill directories, no longer accepts
unsafe `/tmp` runs roots without explicit consent, and no longer emits ambiguous
stub delivery when Telegram routing data is missing.

## Operator-visible changes

- **Canonical path guard** — entrypoints refuse non-canonical skill roots
  (including `*.bak*`, `*.old*`, `*.backup*`, `*.disabled*`, `copy of *`) with
  `RFO-NON-CANONICAL-SKILL-PATH` (exit `11`).
- **Runs-root guard** — `--runs-root` under `/tmp` is denied by default with
  `RFO-RUNS-ROOT-FORBIDDEN` (exit `12`), allowed only with
  `RFO_ALLOW_TMP_RUNS_ROOT=1` for smoke usage.
- **Delivery truth path (historical Telegram adapter)** — when the in-tree adapter
  existed, `chat_id` resolution followed `request -> argv -> env(consent) -> fail`;
  missing routing produced `delivery_not_proven` instead of silent `stub_only`.
- **Verifier extensions** — `verify_skill_run_claims.py` (compat wrapper `verify_openclaw_run.py`) adds:
  `LIE-DETECTED-WRONG-SKILL-PATH`,
  `LIE-DETECTED-WRONG-RUNS-ROOT`,
  `LIE-DETECTED-DELIVERY-STUB-WITHOUT-CONSENT`,
  `LIE-DETECTED-NARRATIVE-WITHOUT-EVIDENCE`.
- **Smoke + repro wrappers** — added replayable v19.2.1 wrappers:
  `_smoke_v19_2_1_honesty.py`, `_smoke_v19_2_1_repro_baseline.py`.

## Migration (historical — fork-only if you restore Telegram adapters)

- `TELEGRAM_API_BASE` in runtime when using an in-tree Bot API adapter.
- Production invocation passing `--chat-id` / `--reply-to-message-id` from the host.
- `TELEGRAM_CHAT_ID` fallback only with explicit consent flags (smoke).

## References

- `docs/release-notes/v19.2.md`
- `docs/adr/ADR-014-telegram-operator-control-plane.md`
- `docs/adr/ADR-015-runtime-truth-restoration.md`
