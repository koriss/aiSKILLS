# kb/ — knowledge bases (non-canonical for RFO runtime)

Files under this directory are **supporting reference material** (e.g. rubrics, IO indices). They are **not** the runtime contract.

**Canonical sources for behavior:** [`SKILL.md`](../SKILL.md), [`runtime/`](../runtime/), [`contracts/`](../contracts/), [`scripts/validate_skill.py`](../scripts/validate_skill.py).

Do not treat KB excerpts as evidence artifacts in validation — see v19 docs on propaganda-io neutrality and source provenance.
