# Release notes — v19.0.1 (patch)

Patch-only cleanup after **v19.0.0 Pragmatic Rigor phase 1** + **v18.7** logical consistency baseline. No new product features.

## Summary

| ID | Change |
|----|--------|
| A | Removed duplicate `legacy/SKILL-overlays/SKILL.md`; `SKILL.md` no longer references a copy-on-disk overlay path. |
| B | `validation-profile-used.json` now records **`options` verbatim** from the selected profile; V1 + V4 read `options.require_full_contradiction_matrix` (removed derived `contradictions_required`). |
| C | `contracts/validator-registry.json` **version → 19.0.1**. |
| D | `docs/v19/migration-map.md` — registry **35** validators, DAG scope **17**, row for `validate_logical_consistency`. |
| E | `validate_logical_consistency.py` docstring LC01–**LC16**; `validate_skill` error key `frontmatter_missing_skill_version`. |
| F | `tests/fixtures/v19/*` regenerated; `run.json.version` **19.0.1**; profile-used matches live profile `options`. |
| H | V2: **`role_for_claim`** must match `schemas/core/claims-registry.schema.json` enum; bad fixture `support_set_role_invalid`. |
| I | V6: **`cli` + `external_delivery_claim_allowed`** → `DELIV-EXT-CLI`; bad fixture `cli_with_external_delivery_claim_allowed`. |
| J | `docs/v19/drafts/README.md` — **FROZEN**; single source of truth = `schemas/core/`, `validation-profiles/`. |
| K | Operator copy only: no `effective_independent_support_count` claim in `SKILL-core.md` (none present). |
| L | Semver **19.0.1** in `runtime/version.json`, `validate_skill.py`, `SKILL.md`, compatibility matrix, operating discipline, this file. |

## Verification

Run `python -S scripts/validate_skill.py`, `python -S scripts/check_schema_drift.py`, `scripts/run_core_validators.py` on good + all bad v19 fixtures, and `scripts/validate_release.py` on the release smoke path per `AGENTS.md` / CI docs.

## Workspace zip

- **`$HOME/_guests/openclaw-research-factory-orchestrator-v19.0.1-workspace.zip`** — excludes `.git`, `__pycache__`, `.pyc`, `.pytest_cache`, `.egg-info`.
