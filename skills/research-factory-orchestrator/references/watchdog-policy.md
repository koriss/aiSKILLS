
# Watchdog Policy

A script watchdog tracks stale/running/failed work units and blocks final delivery until no retryable or unmerged work units remain.

Watchdog state lives in `watchdog-state.json`.
