
# Report Design System Policy

The report must be built from design tokens and reusable semantic components.

Required layers:

```text
design tokens
layout primitives
status system
components
archetypes
renderer
validators
```

Do not create random one-off HTML blocks when a component exists.
