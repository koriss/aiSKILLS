
# Identity Confusion Set

Store similar but rejected people in `identity-confusion-set.json`.

This prevents repeated confusion.

Each entry should include:

- name;
- url;
- why confusable;
- why rejected;
- conflict fields.
