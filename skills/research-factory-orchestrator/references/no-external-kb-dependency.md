# No External KB Dependency

The skill validates that required KB files exist under its own directory.

The default KB path is relative to SKILL.md: `kb/propaganda-io/`.

External paths may be used only as optional overlays and must never be mandatory.
