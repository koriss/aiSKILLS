
# Provenance Manifest Policy

Use a PROV-like model:

```text
Entity = source, claim, evidence card, report, Telegram message, zip
Activity = search, fetch, normalize, score, fact-check, render, validate, deliver
Agent = orchestrator, worker, validator, renderer
```

Every important output must have provenance.
