# Release notes — v19.1.0 (multi-agent verification frontiers + replayable evidence)

**Semver:** minor (additive advisory channels + optional schemas + release-proof smokes; **no** seventh core validator; V1–V6 blocking semantics unchanged).

## Summary table

| Area | What shipped |
|------|----------------|
| A1 | `run_blinded_checker.py` + `validation/blinded-checker-report.json` + transcript `advisory_channels.blinded_checker` |
| A2 | `run_typed_grounding.py` + `validation/typed-grounding-report.json` + `advisory_channels.typed_grounding` |
| A3 | `schemas/core/judge-council.schema.json` + fixtures |
| B1 | `RFO_FIXED_TIME`, `RFO_DETERMINISTIC_IDS`, `RFO_ID_SALT`, `RFO_NO_NETWORK` |
| B2 | `_smoke_deterministic_replay.py` + `DETERMINISTIC-DRIFT` |
| B3 | `run-events.jsonl` (`validator.*`, `artifact.written`, `rollback.triggered`) |
| B4 | `_smoke_trajectory_v19.py` + `TRAJECTORY-SKIP` |
| C1 | Release zip triad from `validate_release.py` |
| C2 | `_smoke_clean_install.py` |
| C3 | `validate_validator_coverage.py` + `coverage-report.json` + `COVERAGE-GAP` |
| D1 | `EXTERNAL-INSTRUCTION-SIGNAL` in V5 + `bad/instruction_injection_in_evidence` |
| D2 | `tools/inject_fixture_noise.py` (dev/CI helper) |
| D3 | `bad/typed_grounding_contradiction_silent_drop` + `TYPED-GROUNDEDNESS-INFLATION` |
| E1 | `build_sacred_path_graph.py` → `validation/sacred-path-graph.json` |
| E2 | `schemas/core/source-policy.schema.json` + V3 `SOURCE-POLICY-UNKNOWN` + fixtures |
| Docs | `ADR-012`, `ADR-013`, `operating-discipline.md` refresh |

## production-hardening-phase1 closure (back-references)

| § | Topic | v19.1 closure |
|---|--------|---------------|
| §1 | Release zip triad | **C1** — `release-artifacts/*.zip` + `.sha256` + `release-manifest.json` from `validate_release.py` |
| §2 | Clean-install smoke | **C2** — `_smoke_clean_install.py` REQUIRED gate |
| §3 | Coverage meta-validator | **C3** — `validate_validator_coverage.py` REQUIRED gate |
| §4 | Trajectory / run events | **B3 + B4** — `run-events.jsonl` + trajectory smoke |
| §6 | Source policy registry | **E2** — `source-policy.json` + schema + V3 advisory |
| §7 | Instruction-injection defense | **D1** — V5 `EXTERNAL-INSTRUCTION-SIGNAL` (warn by default; profile may block) |

## Operator notes

- Default profile `mvr` keeps new signals **advisory** via `blocking_rules` keys (`external_instruction_in_evidence`, `source_policy_unknown`).
- See `docs/adr/ADR-012-multi-agent-verification.md` and `docs/adr/ADR-013-replayable-evidence.md` for rationale and constraints.
