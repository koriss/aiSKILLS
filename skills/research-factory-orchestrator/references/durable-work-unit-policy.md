
# Durable Work Unit Policy

Large tasks must be executed through contract-bound work units.

A work unit is a bounded shard with explicit objective, scope, required artifacts, validators, retry policy, and merge target.

Free-form subagent output is not completion.
