{
  "contract_id": "query-fanout-config",
  "version": "v19.3.0",
  "description": "Deterministic relay query variants for multi-vector prefetch (stub heuristics; expand over time).",
  "templates": [
    "{task}",
    "{task} definition background",
    "{task} controversy dispute",
    "{task} recent news timeline",
    "site:wikipedia.org {task}",
    "{task} official site OR government",
    "{task} analysis independent",
    "{task} risks limitations"
  ],
  "default_max_templates": 8
}
