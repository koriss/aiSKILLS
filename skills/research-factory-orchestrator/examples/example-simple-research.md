# Example: simple research

**User request (paraphrased):** Summarize the current consensus and main disputes on a narrowly scoped technical question, with citations.

**Mode:** `LIGHTWEIGHT_RESEARCH` or default `AUTO_COMPILE_AND_EXECUTE` with a single queue item.

**Expected:** One item, executive summary, limitations, source-backed claims only.
