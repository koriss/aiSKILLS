# Context Loading Worker Prompt

Do not claim full read/load without manifests. Distinguish read, index, load, execute, smoke, and memory-write. Build inventory and read-ledger before any exhaustive-read claim.
