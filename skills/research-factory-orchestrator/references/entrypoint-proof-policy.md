
# Entrypoint Proof Policy

Every run must create `entrypoint-proof.json` before claiming a research pipeline started.

It proves:
- exact entrypoint script;
- invocation mode;
- not plain subagent;
- not SKILL.md imitation;
- run_id;
- startup artifacts;
- startup state.
