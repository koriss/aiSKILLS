# Example: fact-check only

**User request (paraphrased):** Here is a list of public claims; verify each with evidence, correct or remove unsupported parts.

**Mode:** `FACT_CHECK_ONLY` + `AUTO_COMPILE_AND_EXECUTE` (still executes verification pipeline).

**Expected:** Focus on `claims-registry`, fact-check, citation anchors; final output is verification table + limitations.
