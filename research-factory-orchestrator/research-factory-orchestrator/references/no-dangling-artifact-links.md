# No Dangling Artifact Links

Any required artifact path referenced in final outputs must exist inside the research package, inside the skill package, or inside the current workspace output directory.

Do not point required proof artifacts to missing external paths.
