Block final delivery unless summary is from verified claims only, key claims have citation anchors, uncertainty block exists, no unsupported new facts, source list exists if sources were used, artifact links exist if produced, counts are from files, adversarial review is pass or conditional_pass.



Additional mandatory checks:
- complete standalone HTML report exists;
- HTML report contains required full-report sections;
- HTML report is linked or path-referenced in chat;
- HTML report path is recorded in artifact manifest or html-report-delivery.json;
- chat summary does not replace HTML report.



Additional v8 checks:
- research-package.zip exists and validates;
- full source URLs are present in HTML;
- embedded completion proof is present;
- identity-resolution gate passes for person research.
