# Strict Full Pipeline

No lightweight split. No economy mode. No shortcut pipeline.

All research tasks follow:

tool discovery → permission check → queue discovery → search strategy generation → source acquisition → source normalization → source quality → source laundering detection → evidence cards → evidence map → claims registry → draft → fact-check → citation locator → adversarial review → error audit → fix → validation → final-answer gate → chat summary → delivery.

No stage is optional.



Final delivery requires both mandatory chat summary and complete standalone HTML report.
No HTML report = no final delivery.
